# Visualization Agent 백로그

프로젝트 방향:
Visualization Agent는 고정된 비교표만 만드는 도구가 아니라, **사용자 주제와 논문 초안 문맥을 읽고 적절한 시각 자료를 선택·생성하는 에이전트**로 구현한다.

## Backlog 43
시각화 입력 데이터 확인

- 사용자 주제, 선별 논문 데이터, Writer 초안에서 시각화에 활용할 입력을 정리한다.
- 시각화 대상 필드와 초안 섹션 구조를 확정한다.

완료 기준:
- 시각화 입력 필드가 문서화되고, 입력 프로파일 파일이 생성된다.

## Backlog 44
Visualization Agent 역할 정의

- Visualization Agent의 목표를 논문 비교가 아니라 주제·문맥 기반 시각 자료 생성으로 정의한다.
- 어떤 시각 자료를 만들고, 어떤 경우에 만들지 기준을 명시한다.

완료 기준:
- 역할 정의 문서가 작성되고 프로젝트 방향과 일치한다.

## Backlog 45
선행연구 비교표 생성 기능 구현

- 선별 논문의 목적, 방법, 결과, 한계를 비교하는 표를 생성한다.
- 본문 `선행연구 분석` 섹션에 활용 가능한 형태로 저장한다.

완료 기준:
- 비교표가 Markdown과 이미지 형태로 저장된다.

## Backlog 46
연구 흐름도 생성 기능 구현

- 선행연구가 시간 또는 문제의식 흐름 속에서 어떻게 이어지는지 시각화한다.
- 초안의 `선행연구 분석` 또는 `논의` 섹션에서 활용할 수 있게 만든다.

완료 기준:
- 연구 흐름도 이미지가 생성되고 저장된다.

## Backlog 47
연구 분류 체계 도표 생성 기능 구현

- 선행연구를 주제별, 접근법별, 평가 관점별로 분류하는 도표를 생성한다.
- 초안의 `선행연구 분석` 섹션 보조 자료로 활용한다.

완료 기준:
- 연구 분류 도표 이미지가 생성되고 저장된다.

## Backlog 48
실험·정량 근거 비교표 생성 기능 구현

- 논문 내 수치, 실험 범위, 정량 근거가 드러나는 경우 이를 표로 정리한다.
- 초안의 `연구 방법 및 분석 관점` 섹션 보조 자료로 활용한다.

완료 기준:
- 정량 근거 비교표가 Markdown과 이미지 형태로 저장된다.

## Backlog 49
논문 초안 보조 도식 생성 기능 구현

- 주제와 초안 논지를 바탕으로 핵심 주장 구조를 시각적으로 요약한 도식을 생성한다.
- 초안의 `논의` 또는 `결론` 섹션에 활용할 수 있게 만든다.

완료 기준:
- 보조 도식 이미지가 생성되고 저장된다.

## Backlog 50
시각화 결과 저장 기능 구현

- 생성된 표, 그래프, 도표, 이미지를 `outputs/visualizations/`에 저장한다.
- 파일 경로와 종류를 기록한 매니페스트를 함께 저장한다.

완료 기준:
- 시각 자료와 매니페스트가 정상 저장된다.

## Backlog 51
Writer 연계 구조 설계

- Writer 초안의 각 섹션에 어떤 시각 자료가 대응되는지 연결 정보를 설계한다.
- 초안-시각 자료 매핑 파일을 저장한다.

완료 기준:
- Writer 시각 자료 연결 맵이 생성된다.

## Backlog 52
Visualization Agent 테스트 및 검증

- 샘플 주제로 Visualization Agent를 실행한다.
- 시각 자료가 생성되었는지, 저장되었는지, Writer 연계 구조가 생성되었는지 확인한다.

완료 기준:
- 주요 시각 자료 누락 없이 생성되고 테스트가 성공한다.
